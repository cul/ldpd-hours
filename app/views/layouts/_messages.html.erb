<% flash.each do |key, value| %>
  <% key = { 'error' => 'danger', 'notice' => 'info' }.fetch(key, key) %>
  <div class="alert alert-<%= key %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <%= value %>
  </div>
<% end %>
